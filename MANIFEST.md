# PokéGuess Online — MANIFEST
_Last updated: 2026-07-08 · the authoritative CURRENT-STATE file inventory._
_For the history of **why** each change was made, see `CHANGE_TRACKER_v3.md` — this file is just a snapshot of what exists right now, verified directly against the actual file headers._

Per SPEC principle 1, every code file carries a header (`@file`, `@version`,
`@updated`, `@changelog`); versions live in files, never in filenames. This
table is the index. Status: ✅ done & tested · 🟡 in progress · ⬜ not started.

---

## App shell

| File | Version | Status | Notes |
|------|--------:|--------|-------|
| docs/index.html | 1.0.0 | ✅ | Loads fonts + styles.css + main.js (module). Relative paths for Pages subpath. |
| docs/.nojekyll | — | ✅ | Empty; keeps Pages from stripping `js/`-prefixed paths. |
| docs/css/styles.css | 1.14.3 | ✅ | Sections A-P. Latest additions: profile pill/identity panel, Cycling Road v2, Team Mode, .clue-limit-note, Victory Road merged-chip styles, .lb-draft-links, .host-left-banner (online.js + race.js host-disconnect resilience). |
| docs/js/main.js | 1.5.0 | ✅ | Config load, menu render, hash router, lazy launch. Profile pill wiring (identity-ui.js). New: parseHash() supports a query string (#/online/2?code=ABCDEF), threaded through route()/launchMode() into params.query — backs online.js's/race.js's room-invite deep links. |
| docs/js/modes.js | 1.8.0 | ✅ | Mode registry. Cycling Road blurb updated for the v2 (synced-timer) mechanic. |

## Data pipeline & data
_Unchanged since Phase 2 — not touched in the most recent rounds of work._

| File | Version | Status | Notes |
|------|--------:|--------|-------|
| tools/generate-data.mjs | 1.0.0 | ✅ | Excel → data/*.json. |
| docs/data/config.json | 1.0.0 | ✅ | App shell config: title, gens, genLabels, mpDefaults, modes list. |
| docs/data/gen1.json / gen2.json | gen | ✅ | 151 / 251 mons + clues/categories/difficulties/multiClue. |
| docs/data/movelist-gen{1,2}.json | gen | ✅ | Full real-move learnsets (used by the guess game's moveset clues). |
| docs/data/movestats-gen{1,2}.json | curated | ✅ | 244 moves (Gen 2), Gen 1 mirrors Gen 2. Base power/accuracy/type/category/PP/priority only — see sim.js below for where the actual move *effects* live. Fairy-type fix: Charm and Sweet Kiss retagged Normal, Moonlight retagged Dark — their real pre-Gen-6 types (Fairy didn't exist in Gen 1/2; the data pipeline had inherited the modern retcon typing). Confirmed inert for simulator behavior (type-effectiveness is only ever consulted by the damage path, and all three are 0-bp status moves) — a pure data-accuracy fix. |
| docs/data/typechart-gen{1,2}.json | gen | ✅ | 17-type (Gen 2) / 15-type (Gen 1, derived) chart. No Fairy row — correct, since Fairy doesn't exist in Gen 1/2 (see movestats-gen{1,2}.json's entry above: the 3 moves that used to be mistagged Fairy are now retagged to their real historical types). |
| docs/data/draftpool-gen2.json | gen | ✅ | Draft-specific movepool overrides (e.g. Smeargle). |
| database.rules.json | 1.1.0 | ✅ | Firebase security rules. #12/#13: added /draft/progress/{uid} (own-uid-only write, numeric 0-5) backing the persisted Elite-4 unlock-progress fix. NEEDS TO BE RE-DEPLOYED TO THE FIREBASE CONSOLE — this repo copy is ahead of what's live until you re-paste it. |

## Shared libraries (`docs/js/lib/`)

| File | Version | Status | Notes |
|------|--------:|--------|-------|
| dom.js | 1.2.0 | ✅ | el(), clear, statSpreadEl, genBar. No global state. New: shareSheetEl(text, opts) — a shared share-sheet toast (text + WhatsApp/Copy/Close), extracted from draftbattle.js's local showShareSheet so online.js/race.js's room-invite sharing reuse the identical UI. |
| engine.js | 1.3.1 | ✅ | PokeGuessRound — clue costs/reveals/guessing/scoring/difficulty locks, category diversity, multi-use clue exhaustion. Shared by every guess mode + Cycling Road's clue sequencing. #5: added SCORE_MULTIPLIERS + computeScoreMultiplier — Single Player leaderboard scores stack a multiplier for harder settings (Forced Reveal, Random/By-category, stricter category diversity) on top of the difficulty points budget. |
| mp-rules.js | 1.4.0 | ✅ | Pure multiplayer rules: seedFor, buildEngine, applyReveals, revealOutcome, guessOutcome, weightedRandomClue, champion, makeRoomCode, computeAutoDeducedIds (#8: uses the engine's exact evolution determination rules, no longer leaks family size after "Can Evolve = No" alone), buildRevealSequence, makeRng. New: leaderUid(room) — the single shared host-disconnect-resilience helper used by both online.js and race.js, extracted from online.js's prior local implementation so the two controllers can't drift apart. |
| firebase.js | 1.0.0 | ✅ | Lazy Firebase connection (`getFirebase()`). |
| identity.js | 1.1.0 | ✅ | Anonymous auth + display name + PIN claim/re-link. `checkNameClaim`/`getClaimStatus` added for collision-checked name changes. |
| leaderboard-data.js | 1.0.0 | ✅ | submit/read/rank leaderboard entries. |
| share.js | 1.5.0 | ✅ | CT-aware date/period/seed helpers, summary-card text ('daily'/'throne'/'gauntlet' kinds), WhatsApp/clipboard share, the drafted-mon share-card infrastructure (TYPE_COLORS/typeColor/typeTextColor, buildMonCardPlan, drawMonCardToCanvas, draftBattleLink, canvasToPngBlob, shareMonCardImage). #1: daily share text leads with dailyChallengeLink() and shows the player's name (stablePlayerFallbackName if unset) instead of the mon's name. New: roomJoinLink(modeId, gen, code) and buildRoomInviteText({gameLabel, details, link}) — back online.js's/race.js's room-sharing feature. |
| draft-adapter.js | 1.3.0 | ✅ | Thin re-export shim for the spec-locked ../draft.js and ../sim.js — lets mode controllers import them from lib/ without editing the vetted originals. Re-exports include autoDraftScaled, resolveThroneCascade, TIER_RANK, isTierUnlocked, nextProgressRank. |
| identity-ui.js | 1.0.0 | ✅ | Profile pill + full identity panel (set/change name with collision check, PIN protect, re-link on a new device) — replaced the old one-shot first-load toast. |
| catch-tracker.js | 1.0.0 | ✅ | Shared Seen/Caught store — single source of truth used by every guess mode. |
| pokeinfo.js | 1.0.0 | ✅ | Shared Pokedex-detail-card HTML builder — one source of truth so the Pokedex and the guess-mode summary screen render a Pokemon's info identically. |

## Guess modes (`docs/js/modes/`)

| File | Version | Status | Notes |
|------|--------:|--------|-------|
| single.js | 1.2.2 | ✅ | Single Player. Category-mode + diversity support; catch-tracker wired in. #2/#3: corrected greyed-clue help text. #5: leaderboard score now applies engine.js's multiplier stack; summary shows "raw x multiplier = final"; Custom stays unmultiplied and off the leaderboard. |
| pokedex.js | 1.1.0 | ✅ | Independent Seen/Caught filter toggles (union when both active, replacing the old exclusive radio); uses shared catch-tracker + pokeinfo. |
| safari.js | 1.2.0 | ✅ | Bait/rock reveal at normal cost, manual click costs double; catch-tracker wired in. |
| victoryroad.js | 1.4.0 | ✅ | Endless streak gauntlet, 8 tiers. #6 (full tier rework): every tier's threshold +1; habitat extended to Tier 2, First Anime to Tier 3; "Has an Immunity" added to Tiers 1-4; combined weakness/resistance reveal added to Tiers 3-8; Highest/Lowest Base Stat (no value) added to Tier 7; types/egg-moves/weakness-resistance each merged into one chip. Preview screen fixed to match actual gameplay. **Requested reorder:** the ribbon now groups clues by the same 7 categories used throughout the app (Habitat, Evolution, Type Matchups, Stats, Trainer Usage, Movesets, Anime) instead of a somewhat arbitrary order — weaknesses/resistances sit next to type/immunity info, both stat clues sit together, etc. Replaced the old two-array-plus-offset `chipOrder()` with a single unified priority map — the old design added +100 to any clue identified by `field` rather than `special` (habitat, evoStage, bstRange, fullStats, gymLeader), silently pushing ALL of them to the very end regardless of intended group; found while verifying the reorder had actually taken effect, since the ribbon initially looked unchanged despite the new category order being correctly written. |
| multiplayer.js (hot-seat) | 1.3.2 | ✅ | RTG/GTR, Choose/Random/By-Category clue modes, real category diversity, clue-exclusion panel, evolution auto-deduction, catch-tracker wired in. #9: GTR's reveal step is exactly one mandatory clue (was letting the same player reveal unlimited times, or skip with zero reveals) — auto-advances the turn afterward. #7: dropped the layout-dependent "above"/up-arrow hint text (only true in mobile's stacked layout). Removed the "Skip guess / go to reveal" button from GTR's guess phase (undermined the guess-first design). |
| online.js | 1.6.0 | ✅ | Full feature parity with hot-seat: By-Category clue mode, real diversity, exclusion panel, evolution auto-deduction, catch-tracker wired in. #19: GTR's reveal step now advances the turn automatically after exactly one clue (previously stayed in 'reveal' indefinitely after a turn-expiry + wrong guess). #7: dropped the layout-dependent up-arrow hint. Removed the "Skip guess -> reveal" GTR button. Host-disconnect resilience: the Lobby "Start game" and post-game "Start rematch" buttons were still hard-gated on room.hostUid===me.uid (a known gap even here) — both now use isLeader() (shared via mp-rules.leaderUid), and a host-left banner tells every player when the original host has disconnected and who's taken over. isHost() removed (now unused). New room sharing — a Share Room button in the lobby builds an invite (gen, RTG/GTR, win target, then a deep link) via shareSheetEl/roomJoinLink/buildRoomInviteText; opening that link lands directly on the join screen with the code pre-filled. Known gap: clue cards use their own .online-clue CSS rather than hot-seat's .clue-btn styling. |
| leaderboard.js | 1.2.0 | ✅ | Leaderboard browse screen. #11: added Elite 4 Standings / Daily Challenge Results links at the top (draft results live in their own screens, not the score tables here). |

## Cycling Road (`docs/js/modes/race.js`)

| File | Version | Status | Notes |
|------|--------:|--------|-------|
| race.js | 2.3.0 | ✅ | Full rewrite from the ground up — predetermined per-mystery clue order, independent per-player advancement with live standings + toasts, rooms hold up to 12, time cap, results ranked by total time with split highlighting, persistent post-game lobby + opt-in rematch. Team Mode (settings.teams): team-builder lobby, one shared position per team with a rotating answerer, team standings/results, rematch requires every connected player opted in. #17: the shared 1-second cap-timer interval now dispatches team-aware vs individual functions correctly (previously hijacked the standings display in team games and denied the game-ending check a periodic self-heal chance if a reactive update was ever missed). #16: individual room create/join no longer writes team:undefined (Firebase rejects it). Host-disconnect resilience (shared mp-rules.leaderUid() with online.js): every host-gated action (both lobbies' Start buttons, both rematch buttons, turn-timeout/round-ending duties) uses isLeader(); a host-left banner shows everywhere a room exists. Found and fixed along the way: renderTeamGameOver() was missing its own bestByCol/worstByCol declaration — a ReferenceError newly exposed (not introduced) by the #17 fix, which was the first time team mode's own ending logic became reachable via the cap-timer route. New room sharing — a Share Room button in both the individual and team-builder lobbies (same infrastructure as online.js, so the two can't drift apart); opening a shared link lands directly on the pre-filled join screen. |

## Draft Battle

| File | Version | Status | Notes |
|------|--------:|--------|-------|
| docs/js/draft.js | 0.8.0 | ✅ | Lives at docs/js/, not docs/js/lib/ — spec-locked "vetted" file; other modules import it via draft-adapter.js. DraftSession/autoDraft/buildSpeciesList/buildLearnsetMap unchanged in behavior. autoDraftScaled, resolveThroneCascade + TIER_RANK, banned-move filtering. isTierUnlocked + nextProgressRank — the Elite-4 unlock gate is based on a persisted, monotonic "highest tier ever reached" value instead of "do you currently hold the previous throne," which the one-throne cascade and every tier's own cadence reset both legitimately (and wrongly) used to relock. |
| docs/js/sim.js | 2.2.0 | ✅ | Lives at docs/js/, not docs/js/lib/. Real per-move effects via MOVE_EFFECTS (recoil, drain, self-heal, status, confusion, stat boosts, OHKO, high-crit, fixed/HP-based damage), multi-hit, two-turn/recharge moves, move-specific special cases (Curse, Belly Drum, Rest, Pain Split, Leech Seed, Jump Kick crash). Fixed OHKO moves being completely non-functional (bp:0 skipped the damage-dispatch check). Move-accuracy pass: Magnitude rolls the real 4-10 power table every use (MAGNITUDE_TABLE/rollMagnitude, was flat bp:75); Tri Attack's secondary proc picks randomly among paralysis/burn/freeze (was always paralysis); Charm was found to have NO effect implemented at all (a silent no-op) while investigating the Fairy-type fix — added its real -2 Attack drop. Return/Frustration deliberately left at flat power — their real formulas are friendship-based and no friendship stat exists anywhere in this draft context. Jump Kick/High Jump Kick crash left at 1/8 max HP — no more-confident Gen-2-specific figure found. |
| docs/js/lib/draft-adapter.js | 1.3.0 | ✅ | See "Shared libraries" above. |
| docs/js/modes/draftbattle.js | 1.13.1 | ✅ | Draft UI -> draft card -> Elite-4 gauntlet -> daily -> share. Elite-4 gauntlet consolidated into one action. Daily Draft matchups/inspect/Cal rename per the previous batch. **Fixed:** `startDraft()` never passed `playerName` to `DraftSession` at all, so every drafted mon's name defaulted to the literal string "Player" ("Player's Feraligatr") regardless of who was playing — affected free-play, the daily, gauntlet share text, and the daily results table alike. Identity is now resolved IN PARALLEL with the existing movelist/movestats/draftpool/typechart fetches, so the correct name is available from the very first card with no added latency — deliberately not a background correction after the fact, since an earlier version of this fix raced a background identity resolution against how fast a draft could complete and lost that race in testing. |

## Tests (`tools/test/`)

| File | Covers |
|------|--------|
| run.mjs | Zero-dep test runner — node tools/test/run.mjs runs every *.test.mjs suite below (696 assertions total as of this writing). |
| sim.test.mjs | Stat conversion, moveId, recoil/drain/multi-hit/two-turn/recharge/OHKO/high-crit/fixed-damage/guaranteed-status/Curse/Belly Drum/Rest/Pain Split/Leech Seed/Jump Kick crash — the full #6 move-mechanics rework. |
| sim-status.test.mjs | Deep, exact-value verification of status effects and stat stages (burn/poison/toxic chip math, paralysis speed/full-para rate, freeze thaw rate, confusion self-hit rate, stat-boost deltas and clamping), plus the move-accuracy pass: Charm's -2 Attack (was a no-op), Magnitude rolling multiple distinct levels across many uses, Tri Attack's proc producing more than one distinct status. Was written but never registered in run.mjs for a while — none of its assertions ran as part of npm test until that was fixed. |
| draft.test.mjs | Two-picks-from-correct-card, commitCard, type-twice->mono, "--" pick, full-completion, daily determinism, weighted move reroll, autoDraft, the banned-move list, autoDraftScaled's target-band convergence, resolveThroneCascade's full decision matrix, isTierUnlocked/nextProgressRank. |
| engine.test.mjs | normalizeName, round setup, clue purchase/cost, wrong-guess cost, correct-guess score, gen pool filter, computeScoreMultiplier (#5) — every axis, the full stack, Custom returning null. |
| mp-rules.test.mjs | Seed determinism, room codes, replay-identical clue values, reveal/guess outcomes, turn rotation, champion, buildRevealSequence, computeAutoDeducedIds (#8), leaderUid (original host connected, fallback to earliest-joined-still-connected, joinOrder vs Object.keys ordering, never-null-for-a-real-room, null-safe on a missing room). |
| identity.test.mjs | Name claim, cross-device reclaim, wrong-PIN rejection. |
| catch-tracker.test.mjs | Basic get/mark, caught-implies-seen (never downgrades), case-insensitivity, manual override incl. clearing an entry. |
| modes.smoke.mjs | jsdom render smoke for every guess mode; Pokedex combo-filter; Single Player's win/loss catch-tracking; #5 deterministic-win multiplier breakdown; #11 (Leaderboard's Elite 4 Standings / Daily Challenge Results links). **Fixed a real intermittent flake:** the "ran out of points" test bought every affordable clue card, but that doesn't guarantee reaching zero points for every possible random mystery (some mysteries' total available clue cost is less than the difficulty's starting budget) — falls back to repeated wrong guesses (which do cost points) once no more cards are clickable. |
| online.smoke.mjs | Two-client fake-Firebase integration: create->join->start->reveal-sync->scoring->auto-advance->turn-skip, By-Category/diversity/exclusion/evo-deduction parity, rematch flow, #8 evo-deduction non-leak, #19 (GTR after a turn-expiry: exactly one reveal, no skip, auto-advance), host-disconnect resilience (lobby Start button, banner, mid-game duty with the original host disconnected), room sharing (Share Room invite text + link, and the link's code param pre-filling the join screen). |
| cluemode.smoke.mjs | Single Player's Random/By-Category/diversity-blocked card states + #4 (clue-limit-note class-CSS binding). |
| mp-cluemode.smoke.mjs | Hot-seat's Random/By-Category/diversity-blocked card states + the multi-use clue re-offer fix + #9 (GTR: exactly one reveal, no skip, auto-advance). **Fixed a real intermittent flake:** the Random/By-category "did it reveal something" checks counted `.clue-btn.revealed` elements, but a multi-use clue (e.g. "Reveal One Egg Move", cheap and thus heavily favored by the weighted-random formula) never gets that CSS class after just one use — only once fully exhausted. Now counts the revealed-clues panel's entries instead, which is correct regardless of clue type. |
| identity-ui.smoke.mjs | Profile pill + identity panel (name change, PIN protect, re-link, collision blocking). |
| race.smoke.mjs | Cycling Road individual mode end-to-end: predetermined sync, independent pacing + toasts, room cap, time cap, results/splits, both rematch outcomes, early exit, #16, #18 investigation (could not reproduce; see CHANGE_TRACKER), host-disconnect resilience, room sharing (Share Room invite + pre-filled join link). Uses a virtual clock + per-client JSDOM windows; fake Firebase's reentrancy guard queues a nested notification instead of dropping it. |
| race-teams.smoke.mjs | Team Mode end-to-end: room creation, both team-assignment paths, the answerer gate + rotation, cross-team-only toasts, dual-team completion, results, all-opt-in rematch, #17, host-disconnect resilience (team-builder lobby taken over by a fallback leader), room sharing (Team Mode invite mentions Team Mode). Same reentrancy-queueing fake-Firebase fix as race.smoke.mjs. |
| victoryroad.smoke.mjs | #6's full tier rework driven through the real UI with a deterministic shuffle (rng:()=>0): Tier 1->2 boundary exactly at streak 5/6, "Has an Immunity" pre-revealed in Tier 1, the merged Type/Egg-Move/Type-Matchups chips, no exhaustion-sentinel leaks, reaching Tier 3 (streak 11) to confirm the combined weakness/resistance reveal, and the pre-game tier preview matching actual gameplay. **New:** the category-grouped ribbon reorder — Cat 1/2 before Cat 3 (Type Matchups), weakness/resistance sitting immediately next to Has an Immunity/Type, the two stat clues adjacent, Trainer Usage between Stats and Movesets, Anime last. |
| daily.smoke.mjs | New. Daily Draft's individual head-to-head matchups (every other entrant including Cal, no self-battling), on-demand battle replay via the existing playback UI reusing the already-computed sample log (verified the replayed verdict always agrees with the win/loss already shown in the matchups list — proving no re-simulation), the read-only mon "inspect" view (types/stats/moves, no draft-action buttons), and the Daily Rival -> Cal rename. Found and caught a real bug: renderBattle()'s verdict text was hardcoded to the challenger side, showing the WRONG winner when viewing a matchup from the other player's perspective — fixed via a new viewingSide option. |
| throne.smoke.mjs | Drives the real Elite-4 gauntlet end-to-end (#14/#15) against a real fake Firebase via draftbattle.js's test-injection hooks: one-click Will->All-Time climb, per-row "Watch" replay + return to results, one Claim, one Share, the "Your best" progress badge surviving the #14a cascade AND a simulated cadence reset (#12/#13), claimThrone's write-verification, and the daily "already done today" gate's View Results button showing today (#10). **New:** the drafted mon's name uses the player's real screen name ("Ash's...") for both free-play and the daily flow, not the literal fallback string "Player's...". |
| share.test.mjs | CT date/period/seed helpers; buildSummaryText's 'gauntlet'/'daily'/'throne' kinds; typeColor/typeTextColor spot-checked against the literal styles.css values; buildMonCardPlan's pure layout data; drawMonCardToCanvas verified against a recording fake 2D context (jsdom has no real canvas 2D, and this project avoids a canvas-polyfill dependency); draftBattleLink/dailyChallengeLink's graceful no-location fallback; stablePlayerFallbackName/dailyChallengeLink (#1). |

**Dev-only standalone smoke script** (not part of run.mjs/test:smoke; exercises the full Draft Battle / daily UI with a fake OFFLINE Firebase, i.e. no other entrants — daily.smoke.mjs above covers the multi-entrant matchup scenario): smoke-daily.mjs — also verifies the Yesterday/Today results round-trip and the #1 share-link format. Note: earlier notes also referenced smoke.mjs/smoke2.mjs, but neither exists in the current repo — that reference was already stale; throne.smoke.mjs/daily.smoke.mjs now cover what those were described as testing.

## Rules, docs, config

| File | Status | Notes |
|------|--------|-------|
| database.rules.json | ✅ | See "Data pipeline" above. |
| README.md | ✅ | Rewritten: intro paragraph covers Cycling Road (incl. Team Mode + host-disconnect resilience + room sharing), the Elite-4 gauntlet, and real move mechanics; repository layout tree updated to every current file; testing section documents both npm test and npm run test:smoke accurately. |
| CHANGE_TRACKER_v3.md | ✅ | The authoritative decision history — start here in any new chat. |
| TESTING_CHECKLIST.md | ✅ | Hands-on QA checklist, rewritten for this round's features: Daily Draft matchups/inspect/Cal rename, multiplayer room sharing (online + both Cycling Road modes), the Victory Road tier-preview fix, plus everything from the prior round (VR tier rework, GTR fixes, host-disconnect resilience, leaderboard draft links, daily share format, Elite-4 gauntlet flow, move-accuracy fixes, Fairy-type removal). |
| NEW_CHAT_GUIDE.md | ✅ | How to hand off to a fresh chat without losing context. |

---

## Known, disclosed gaps (not oversights — intentionally scoped out and documented)
- Online's clue cards don't visually match hot-seat's card styling yet (behavior is identical).
- Return/Frustration use flat listed power rather than a real friendship-based formula — no friendship stat exists anywhere in this draft context to compute one from.
- Jump Kick/High Jump Kick's crash damage (1/8 max HP) is an estimate — no more Gen-2-specific figure found with higher confidence.
- Silhouette sprite assets (docs/img/silhouettes/<num>.png) were still owed by the user as of the original Phase 5 notes — the app degrades gracefully without them, but if they were never supplied, Pokedex/Draft silhouettes are still blank.
- #18 (a reported multiplayer rematch bug) was investigated extensively across two rounds but never reproduced by either side, and has been dropped.
- The daily results table gained a 5th (actions) column this round — worth a visual/mobile-width check since it hasn't been seen on a real device yet.
