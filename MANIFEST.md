# PokéGuess Online — MANIFEST
_Last updated: 2026-07-12 (batch 3) · the authoritative CURRENT-STATE file inventory._
_For the history of **why** each change was made, see `CHANGE_TRACKER_v3.md` — this file is just a snapshot of what exists right now, verified directly against the actual file headers._

Per SPEC principle 1, every code file carries a header (`@file`, `@version`,
`@updated`, `@changelog`); versions live in files, never in filenames. This
table is the index. Status: ✅ done & tested · 🟡 in progress · ⬜ not started.

---

## App shell

| File | Version | Status | Notes |
|------|--------:|--------|-------|
| docs/index.html | 1.0.0 | ✅ | Loads fonts + styles.css + main.js (module). Relative paths for Pages subpath. |
| docs/.nojekyll | — | ✅ | Empty; keeps Pages from stripping `js/`-prefixed paths. |
| docs/css/styles.css | 1.14.7 | ✅ | Sections A-P. Latest additions: profile pill/identity panel, Cycling Road v2, Team Mode, .clue-limit-note, Victory Road merged-chip styles, .lb-draft-links, .host-left-banner, .sf-intro centering fix, VR ribbon layout fix, shell-footer removal. **New:** .lb-sort-row (Victory Road leaderboard's Total-caught / Avg-time-per-catch sort toggle). |
| docs/js/main.js | 1.5.1 | ✅ | Config load, menu render, hash router, lazy launch. Profile pill wiring (identity-ui.js). parseHash() supports a query string (#/online/2?code=ABCDEF), threaded through route()/launchMode() into params.query — backs online.js's/race.js's room-invite deep links. **Removed the "Build skeleton v1.0.0" footer** (a leftover from the initial app shell), along with its now-unused APP_VERSION constant and the orphaned .shell-footer CSS rule. |
| docs/js/modes.js | 1.8.0 | ✅ | Mode registry. Cycling Road blurb updated for the v2 (synced-timer) mechanic. |

## Data pipeline & data
_Unchanged since Phase 2 — not touched in the most recent rounds of work._

| File | Version | Status | Notes |
|------|--------:|--------|-------|
| tools/generate-data.mjs | 1.0.0 | ✅ | Excel → data/*.json. |
| docs/data/config.json | 1.0.0 | ✅ | App shell config: title, gens, genLabels, mpDefaults, modes list. |
| docs/data/gen1.json / gen2.json | gen | ✅ | 151 / 251 mons + clues/categories/difficulties/multiClue. |
| docs/data/movelist-gen{1,2}.json | gen | ✅ | Full real-move learnsets (used by the guess game's moveset clues). |
| docs/data/movestats-gen{1,2}.json | curated | ✅ | 244 moves (Gen 2), Gen 1 mirrors Gen 2. Base power/accuracy/type/category/PP/priority only — see sim.js below for where the actual move *effects* live. Fairy-type fix: Charm and Sweet Kiss retagged Normal, Moonlight retagged Dark — their real pre-Gen-6 types (Fairy didn't exist in Gen 1/2; the data pipeline had inherited the modern retcon typing). Confirmed inert for simulator behavior (type-effectiveness is only ever consulted by the damage path, and all three are 0-bp status moves) — a pure data-accuracy fix. **Base-power corrections:** Fury Cutter 40→10 and Snore 50→40 (both inflated pre-mechanic), and Return/Frustration 50→102 (each move's power at its own optimal happiness — no happiness stat exists in this context); bp is data-driven throughout, so the values live in data and the mechanics live in sim.js. |
| docs/data/typechart-gen{1,2}.json | gen | ✅ | 17-type (Gen 2) / 15-type (Gen 1, derived) chart. No Fairy row — correct, since Fairy doesn't exist in Gen 1/2 (see movestats-gen{1,2}.json's entry above: the 3 moves that used to be mistagged Fairy are now retagged to their real historical types). |
| docs/data/draftpool-gen2.json | gen | ✅ | Draft-specific movepool overrides (e.g. Smeargle). |
| database.rules.json | 1.1.0 | ✅ | Firebase security rules. #12/#13: added /draft/progress/{uid} (own-uid-only write, numeric 0-5) backing the persisted Elite-4 unlock-progress fix. NEEDS TO BE RE-DEPLOYED TO THE FIREBASE CONSOLE — this repo copy is ahead of what's live until you re-paste it. |

## Shared libraries (`docs/js/lib/`)

| File | Version | Status | Notes |
|------|--------:|--------|-------|
| dom.js | 1.2.0 | ✅ | el(), clear, statSpreadEl, genBar. No global state. New: shareSheetEl(text, opts) — a shared share-sheet toast (text + WhatsApp/Copy/Close), extracted from draftbattle.js's local showShareSheet so online.js/race.js's room-invite sharing reuse the identical UI. |
| engine.js | 1.3.3 | ✅ | PokeGuessRound — clue costs/reveals/guessing/scoring/difficulty locks, category diversity, multi-use clue exhaustion. Shared by every guess mode + Cycling Road's clue sequencing. #5: SCORE_MULTIPLIERS + computeScoreMultiplier. **Fixed a real bug report:** "Reveal One Example Moveset" could show the same move twice (e.g. Mr. Mime's real data lists Thunderbolt twice -- once via an RBY TM import, once via Move Tutor -- and 6 other species have similar source-duplicated moves). allMovesPool pools across every source (unlike tmhms/eggs, which already dedupe since they each filter to one source), so without its own dedup, a shuffled sample could repeat a move. Deduplicated by move name. **Simplified two clue values to plain Yes/No per request:** "Has an Immunity" (was "Yes — has at least one immunity" / "No — no type immunities") and "Used by E4/Red/Rival" (was "No — not used by Elite Four, Red, or Rival"). |
| mp-rules.js | 1.5.0 | ✅ | Pure multiplayer rules: seedFor, buildEngine, applyReveals, revealOutcome, guessOutcome, weightedRandomClue, champion, makeRoomCode, computeAutoDeducedIds (#8: uses the engine's exact evolution determination rules, no longer leaks family size after "Can Evolve = No" alone), buildRevealSequence, makeRng, leaderUid(room) — the single shared host-disconnect-resilience helper used by both online.js and race.js. New: `makeServerNow(fb)` — a shared factory wrapping firebase.js's server-aligned clock (falling back to Date.now() when fb is absent/predates serverNow, e.g. a test fake). Lives here, shared, so every cross-device mode (online, Cycling Road individual + team, and any future one) aligns clocks identically instead of re-defining its own guard — replaced the two identical local serverNow definitions that had been in race.js and online.js. |
| firebase.js | 1.1.0 | ✅ | Lazy Firebase connection (`getFirebase()`). New: `serverNow()`/`serverTimeOffset()` — subscribes to `.info/serverTimeOffset` and exposes a server-aligned clock so every client agrees on "now" regardless of local device-clock skew. Backs the cross-device countdown fixes in race.js/online.js (rematch, round transitions, RTG turn timers). |
| identity.js | 1.1.0 | ✅ | Anonymous auth + display name + PIN claim/re-link. `checkNameClaim`/`getClaimStatus` added for collision-checked name changes. |
| leaderboard-data.js | 1.1.0 | ✅ | submit/read/rank leaderboard entries. **New (requested):** an optional numeric secondary metric (+ metricLabel) can be submitted alongside score; rankEntries/topEntries gained a sort option (sortBy:'metric', metricAsc) so a board can rank by the metric instead of score — backs Safari's catch-per-100-points ranking and Victory Road's optional time-per-catch sort. Entries without a metric sink to the bottom under a metric sort. |
| share.js | 1.5.1 | ✅ | CT-aware date/period/seed helpers, summary-card text ('daily'/'throne'/'gauntlet' kinds), WhatsApp/clipboard share, the drafted-mon share-card infrastructure. #1: daily share text leads with dailyChallengeLink()/player name. roomJoinLink/buildRoomInviteText back online.js's/race.js's room-sharing. **Fixed:** "See if you can beat my Ash's Kangaskhan" had two possessives clashing — the gauntlet/throne "my {mon}" phrases now strip the "PlayerName's " prefix, reading "beat my Kangaskhan" instead. Other uses of monName (e.g. "My Elite 4 challenger: X") aren't a possessive clash and are untouched. |
| draft-adapter.js | 1.3.0 | ✅ | Thin re-export shim for the spec-locked ../draft.js and ../sim.js — lets mode controllers import them from lib/ without editing the vetted originals. Re-exports include autoDraftScaled, resolveThroneCascade, TIER_RANK, isTierUnlocked, nextProgressRank. |
| identity-ui.js | 1.0.0 | ✅ | Profile pill + full identity panel (set/change name with collision check, PIN protect, re-link on a new device) — replaced the old one-shot first-load toast. |
| catch-tracker.js | 1.0.0 | ✅ | Shared Seen/Caught store — single source of truth used by every guess mode. |
| pokeinfo.js | 1.0.0 | ✅ | Shared Pokedex-detail-card HTML builder — one source of truth so the Pokedex and the guess-mode summary screen render a Pokemon's info identically. |

## Guess modes (`docs/js/modes/`)

| File | Version | Status | Notes |
|------|--------:|--------|-------|
| single.js | 1.2.3 | ✅ | Single Player. Category-mode + diversity support; catch-tracker wired in. #2/#3: corrected greyed-clue help text. #5: leaderboard score now applies engine.js's multiplier stack. **Fixed:** the in-game "Reveal Full Stat Spread" clue showed a bare number string with no HP/Atk/Def/... labels, unlike the labeled version already used on the post-game summary screen — now uses the same shared statSpreadEl in-game too. |
| pokedex.js | 1.1.0 | ✅ | Independent Seen/Caught filter toggles (union when both active, replacing the old exclusive radio); uses shared catch-tracker + pokeinfo. |
| safari.js | 1.5.0 | ✅ | Bait/rock reveal at normal cost, manual click costs double; catch-tracker wired in. endGame() crash fix, random-clue safeguard, stat-spread labels, post-game Caught/Ran-From name lists, budget minimum lowered to 1 per prior batch. **New (requested):** leaderboard submission includes the catch-per-100-points efficiency as a sortable metric (Safari boards rank by it), shown in the detail text too. |
| victoryroad.js | 1.5.0 | ✅ | Endless streak gauntlet, 8 tiers. #6 (full tier rework, category-grouped ribbon reorder), intro-paragraph wrap fix per prior batches. **New (requested):** leaderboard submission includes a time-per-catch metric (avg ms per successful guess, lower is better); total caught (score) remains the DEFAULT board sort, with a toggle on the leaderboard screen to re-sort by the new metric. |
| multiplayer.js (hot-seat) | 1.3.4 | ✅ | RTG/GTR, Choose/Random/By-Category clue modes, real category diversity, clue-exclusion panel, evolution auto-deduction, catch-tracker wired in. Stat-spread label fix per prior batch. **Fixed a real bug report:** doGuess() never validated the guess against the actual Pokemon name list (unlike single.js/safari.js, which already had this check) -- any text was accepted as a valid-but-wrong guess, incrementing guess counts and deducting the shared pool's guess cost even for nonsense input. Now shows "Pick a Pokemon from the list" and returns without penalty. |
| online.js | 1.7.0 | ✅ | Full feature parity with hot-seat. Host-disconnect resilience, room sharing, stat-spread label fix, guess-validation fix per prior batches. **Two cross-device timer fixes:** (a) the round-over "Next round in Xs" countdown was rendered once and never refreshed (the ticker only updated the turn timer + rematch countdown), so it sat frozen at its initial value on every device until the round advanced — gave it a stable class (.online-nextround-countdown) and refresh it each tick; (b) turn deadlines / round-transition / rematch countdowns are now written and read via a server-aligned clock (mp-rules makeServerNow → firebase serverNow) instead of each device's own Date.now() — fixes RTG turn timers reading ~1-2s apart between devices, and the rematch clocks not reaching 0 together. Metadata-only stamps (updatedAt, joinedAt) keep Date.now(); never compared across devices. Known gap: clue cards use their own .online-clue CSS rather than hot-seat's .clue-btn styling. |
| leaderboard.js | 1.3.0 | ✅ | Leaderboard browse screen. #11: added Elite 4 Standings / Daily Challenge Results links at the top (draft results live in their own screens, not the score tables here). **New (requested):** Safari boards rank by the catch-per-100-points metric (own column); Victory Road defaults to ranking by total caught but gains a sort toggle to re-rank by average time per catch. Per-mode SORT_CONFIG drives all three board types. |

## Cycling Road (`docs/js/modes/race.js`)

| File | Version | Status | Notes |
|------|--------:|--------|-------|
| race.js | 2.4.1 | ✅ | Full rewrite from the ground up — predetermined per-mystery clue order, independent per-player advancement with live standings + toasts, rooms hold up to 12, time cap, results ranked by total time with split highlighting, persistent post-game lobby + opt-in rematch. Team Mode (settings.teams): team-builder lobby, one shared position per team with a rotating answerer, team standings/results, rematch requires every connected player opted in. Host-disconnect resilience (shared mp-rules.leaderUid() with online.js). Room sharing. Cross-device clock fix (mp-rules makeServerNow → firebase serverNow) fixing the reported "stuck" rematch countdown from clock skew. **New:** rematch-counter grammar fix ("1 player want" → "1 player wants"). A new smoke test reproduces the reported "guest opts in alone" scenario and confirms the counter + host Start button work correctly on both screens — the reported bug is a stale-upload symptom, not a logic bug in current code. |

## Draft Battle

| File | Version | Status | Notes |
|------|--------:|--------|-------|
| docs/js/draft.js | 0.9.3 | ✅ | Lives at docs/js/, not docs/js/lib/ — spec-locked "vetted" file; other modules import it via draft-adapter.js. DraftSession/autoDraft/buildSpeciesList/buildLearnsetMap unchanged in behavior. autoDraftScaled, resolveThroneCascade + TIER_RANK, banned-move filtering, isTierUnlocked + nextProgressRank. **BANNED_DRAFT_MOVES updates:** added Destiny Bond, Sleep Talk, Future Sight, Heal Bell, Psych Up (per the move-audit rule of thumb: faint/switch/nested-execution/delayed-timing moves — and two tiny-win status moves — that don't fit a switchless 1v1 sim are banned rather than modeled); removed Snore (implemented asleep-only, sim 2.7.0) and Mist (implemented, sim 2.10.0). Perish Song stays banned. No Pokemon can appear more than once in a single draft — `_speciesAt()` now excludes species already shown earlier in the same draft (tracked in `_seenSpecies`, populated by `_refresh()` the moment a card is displayed; a rerolled-past card counts as "shown" too). History-dependent rather than a pure function of (position, pokeReroll) alone, but replaying the same seed still reproduces the same sequence. Changed `WINNING_SEED` in throne.smoke.mjs. |
| docs/js/sim.js | 2.11.0 | ✅ | Lives at docs/js/, not docs/js/lib/. Real per-move effects via MOVE_EFFECTS. Through 2.4.0: recoil/drain/heal/status/confuse/boosts/OHKO/high-crit/fixed & HP-based damage, multi-hit, two-turn/recharge, Curse/Belly Drum/Rest/Pain Split/Leech Seed/Jump Kick crash, the sleep-duration fix, Reflect/Light Screen, accuracy/evasion stages, Magnitude roll, Tri Attack, Charm, Icy Wind/Bubble speed drops. **Tier-1 (2.5.0):** Dynamicpunch confuse, Mud-Slap/Octazooka accuracy-drop secondaries, Bone Club flinch, Endure, Protect/Detect, Haze. **Tier-2 (2.6.0):** Nightmare, Safeguard (status+confuse immunity), Lock-On/Mind Reader (hits through Fly/Dig invuln), Fury Cutter/Rollout ramp (×2/hit, cap ×16) + Rollout's forced 5-turn lock, Fly/Dig invuln exceptions (Gust/Twister vs Fly, Earthquake/Magnitude vs Dig, 2×). **Tier-3 (2.7.0–2.8.0):** Snore (asleep-only); Outrage/Thrash/Petal Dance rampage (2–3 turn lock → self-confusion). **Simplified-moves pass (2.9.0–2.11.0):** Bone Rush 2–5 multi-hit; Low Kick 30% flinch (gen 1/2 is flinch, NOT weight-based); Return/Frustration bp→102 (optimal-happiness); trapping Wrap/Bind/Fire Spin/Clamp/Whirlpool (2–5 turn 1/16 chip); Mist (blocks foe stat drops 5 turns); Weather Rain/Sun/Sandstorm (field-level: Water/Fire ×1.5/×0.5, sandstorm 1/16 chip sparing Rock/Ground/Steel, Solar Beam instant in sun, Thunder rain-no-miss/sun-50%, Synthesis-family weather-scaled heal); Substitute (1/4-HP decoy soaks damage + blocks status/stat-drops/seed/trap via a hadSub snapshot; residual chip still hits the mon). Banned rather than modeled (draft.js): Explosion/Self-Destruct, Destiny Bond, Sleep Talk, Future Sight, Disable, Encore, Heal Bell, Psych Up, Perish Song, and the switch/item/PP/gender/copy families. **Disclosed approximations:** Return/Frustration optimal-happiness power; Jump Kick crash 1/8; Hidden Power fixed Normal 60 (no DV/IV data for the real variable type/power — the "Hidden Power (X)" random-type idea needs draft-layer/UI work, deferred); Solar Beam not weakened in rain/sand; Substitute drain/recoil use computed (not sub-absorbed) damage, Pain Split and confusion-self-hit ignore the sub. |

| docs/js/lib/draft-adapter.js | 1.3.0 | ✅ | See "Shared libraries" above. |
| docs/js/modes/draftbattle.js | 1.15.8 | ✅ | Draft UI -> draft card -> Elite-4 gauntlet -> daily -> share. Elite-4 gauntlet consolidated into one action. Daily Draft matchups/inspect/Cal rename, "Player's" name fix, throne cascade fix, sharing UX overhaul per prior batches. Elite-4 stat bands narrowed (Will 425-450, Koga 455-480, Bruno 485-510, Lance 515-540); Throne History Inspect button + `moves` field per prior batch. **Battle-log narration kept in lockstep with sim.js:** Reflect/Light Screen + accuracy/evasion (1.15.1); Endure/Protect/Haze (1.15.2); Nightmare/Safeguard/Lock-On/ramp (1.15.3); Snore asleep-acts (1.15.4); rampage start/end (1.15.5); trapping (1.15.6); Mist + Weather (1.15.7); Substitute (1.15.8). Each new sim event type needs a case here or the renderer's default:continue silently drops it — a recurring bug class this project has hit repeatedly. |

## Tests (`tools/test/`)

| File | Covers |
|------|--------|
| run.mjs | Zero-dep test runner — node tools/test/run.mjs runs every *.test.mjs suite below (696 assertions total as of this writing). |
| sim.test.mjs | Stat conversion, moveId, recoil/drain/multi-hit/two-turn/recharge/OHKO/high-crit/fixed-damage/guaranteed-status/Curse/Belly Drum/Rest/Pain Split/Leech Seed/Jump Kick crash — the full #6 move-mechanics rework. |
| sim-status.test.mjs | Deep, exact-value verification of status effects and stat stages (burn/poison/toxic chip math, paralysis speed/full-para rate, freeze thaw rate, confusion self-hit rate, stat-boost deltas and clamping), plus the move-accuracy pass: Charm's -2 Attack (was a no-op), Magnitude rolling multiple distinct levels across many uses, Tri Attack's proc producing more than one distinct status. **New (battle-mechanics deep-dive batch):** SLEEP costs real turns — a deterministic Rest-based guard (Rest is always exactly 2 sleep turns) catches the reported "wake-and-act-same-turn" regression exactly (buggy code gives 1 miss instead of 2). Reflect/Light Screen halve the correct damage category (physical/special respectively) and leave the other category untouched, verified against damage with no screen up. Accuracy/evasion stages: a 100%-accuracy move never misses neutral evasion but DOES miss a Double-Teaming foe; Sand-Attack's target-accuracy drop causes real misses. Speed/turn-order: Quick Attack (priority) beats a faster foe for that turn, Extreme Speed beats Quick Attack, Agility's Speed boost persists and flips turn order in all future turns, Icy Wind both damages AND drops the target's Speed. |
| draft.test.mjs | Two-picks-from-correct-card, commitCard, type-twice->mono, "--" pick, full-completion, daily determinism, weighted move reroll, autoDraft, the banned-move list, autoDraftScaled's target-band convergence, resolveThroneCascade's full decision matrix, isTierUnlocked/nextProgressRank. **New:** no-duplicate-species-per-draft (#2) — drives ~200 full drafts (30 seeds with rerolls, 61 seeds with them exercised, plus a manually-stepped session) recording every card's species and confirming none repeat; found via a revert-check that a smaller (30-seed) sample wasn't reliably large enough to guarantee catching the regression, since not every seed happens to draw a collision. |
| engine.test.mjs | normalizeName, round setup, clue purchase/cost, wrong-guess cost, correct-guess score, gen pool filter, computeScoreMultiplier (#5). **New:** the example-moveset duplicate-move bug report, using Mr. Mime's REAL movelist data (confirmed it genuinely lists Thunderbolt twice) rather than a synthetic case -- checks the pool itself directly (a shuffle-based check would only have ~0.3% odds per reveal of landing both duplicate positions in the same 4-slice with Mr. Mime's 63-move pool, making it an unreliable regression guard). **New:** the plain-Yes/No format for the "Has an Immunity" and "Used by E4/Red/Rival" clues (Gengar for the Yes case, Bulbasaur for the No case -- Rattata is invalid as a "No" since Normal-type gives it a Ghost immunity). |
| mp-rules.test.mjs | Seed determinism, room codes, replay-identical clue values, reveal/guess outcomes, turn rotation, champion, buildRevealSequence, computeAutoDeducedIds (#8), leaderUid (original host connected, fallback to earliest-joined-still-connected, joinOrder vs Object.keys ordering, never-null-for-a-real-room, null-safe on a missing room). **New:** makeServerNow — delegates to fb.serverNow() when present, reads a live offset fresh each call (not captured once), and falls back to Date.now() for both a null fb and an fb without serverNow (e.g. a test fake). |
| identity.test.mjs | Name claim, cross-device reclaim, wrong-PIN rejection. |
| catch-tracker.test.mjs | Basic get/mark, caught-implies-seen (never downgrades), case-insensitivity, manual override incl. clearing an entry. |
| share.test.mjs | CT date/period/seed helpers; buildSummaryText's 'gauntlet'/'daily'/'throne' kinds; typeColor/typeTextColor; buildMonCardPlan/drawMonCardToCanvas; draftBattleLink/dailyChallengeLink; stablePlayerFallbackName (#1). The "beat my X's Y" / "with my X's Y" possessive-clash fix — confirms the "PlayerName's " prefix is stripped in both the gauntlet and throne kinds, that a monName with no prefix at all is left untouched, and that the OTHER (non-possessive) use of monName is deliberately NOT stripped. |
| leaderboard-data.test.mjs | New. rankEntries' sort behavior: default score-descending (with the earliest-`at` tie-break unchanged), sortBy:'metric' ranking by the metric value instead of score (both higher-is-better and metricAsc/lower-is-better directions), entries without a metric sinking to the bottom under a metric sort (falling back to score order when NO entries have one), the `n` limit, and filtering out entries with a missing/non-numeric score. |
| modes.smoke.mjs | jsdom render smoke for every guess mode; Pokedex combo-filter; Single Player's win/loss catch-tracking; #5 multiplier breakdown; #11 leaderboard links. Fixed the "ran out of points" flake. Labeled stat-spread check for single.js/safari.js; Safari's endGame() crash fix verification; Safari's Bait/Rock budget-floor safeguard (precisely engineered via a throwaway probe session to find the exact edge-case cost, rather than hoping a range sweep lands on it). **New:** Safari's post-game Caught/Ran-From name lists, including the "last mon counts as run from" edge case (identifies mysteries by cross-referencing the Full Stat Spread reveal against real pokedex data, rather than brute-forcing up to 251 names). **New:** the starting-budget minimum is 1, not 50. |
| online.smoke.mjs | Two-client fake-Firebase integration: create->join->start->reveal-sync->scoring->auto-advance->turn-skip, By-Category/diversity/exclusion/evo-deduction parity, rematch flow, #8, #19, host-disconnect resilience, room sharing, stat-spread label check, guess-validation bug report. **New:** the RTG turn-timer cross-device agreement (both clients display the same seconds-left from the shared deadline and count down together) and the round-over "Next round in Xs" countdown ticking down on both clients rather than freezing at its initial value (verified via the harness's controllable clock; revert-check confirmed it catches the frozen-countdown regression). |
| cluemode.smoke.mjs | Single Player's Random/By-Category/diversity-blocked card states + #4 (clue-limit-note class-CSS binding). |
| mp-cluemode.smoke.mjs | Hot-seat's Random/By-Category/diversity-blocked card states + the multi-use clue re-offer fix + #9, stat-spread label check. **New:** the guess-validation bug report -- an invalid guess is flagged as an error (checked via the persisting error class, since this test's mocked synchronous setTimeout clears the message text itself immediately), does not advance the turn, does not deduct from the pool. Existing wrong-guess calls updated to use a real Pokemon name ("Metapod"), since a fake string is now correctly rejected. |
| identity-ui.smoke.mjs | Profile pill + identity panel (name change, PIN protect, re-link, collision blocking). |
| race.smoke.mjs | Cycling Road individual mode end-to-end: predetermined sync, independent pacing + toasts, room cap, time cap, results/splits, both rematch outcomes, early exit, #16, #18 investigation (could not reproduce; see CHANGE_TRACKER), host-disconnect resilience, room sharing (Share Room invite + pre-filled join link). Uses a virtual clock + per-client JSDOM windows; fake Firebase's reentrancy guard queues a nested notification instead of dropping it. |
| race-teams.smoke.mjs | Team Mode end-to-end: room creation, both team-assignment paths, the answerer gate + rotation, cross-team-only toasts, dual-team completion, results, all-opt-in rematch, #17, host-disconnect resilience (team-builder lobby taken over by a fallback leader), room sharing (Team Mode invite mentions Team Mode). Same reentrancy-queueing fake-Firebase fix as race.smoke.mjs. |
| victoryroad.smoke.mjs | #6's full tier rework driven through the real UI with a deterministic shuffle (rng:()=>0): Tier 1->2 boundary exactly at streak 5/6, "Has an Immunity" pre-revealed in Tier 1, the merged Type/Egg-Move/Type-Matchups chips, no exhaustion-sentinel leaks, reaching Tier 3 (streak 11) to confirm the combined weakness/resistance reveal, and the pre-game tier preview matching actual gameplay. **New:** the category-grouped ribbon reorder — Cat 1/2 before Cat 3 (Type Matchups), weakness/resistance sitting immediately next to Has an Immunity/Type, the two stat clues adjacent, Trainer Usage between Stats and Movesets, Anime last. |
| daily.smoke.mjs | New. Daily Draft's individual head-to-head matchups (every other entrant including Cal, no self-battling), on-demand battle replay via the existing playback UI reusing the already-computed sample log (verified the replayed verdict always agrees with the win/loss already shown in the matchups list — proving no re-simulation), the read-only mon "inspect" view (types/stats/moves, no draft-action buttons), and the Daily Rival -> Cal rename. Found and caught a real bug: renderBattle()'s verdict text was hardcoded to the challenger side, showing the WRONG winner when viewing a matchup from the other player's perspective — fixed via a new viewingSide option. |
| throne.smoke.mjs | Drives the real Elite-4 gauntlet end-to-end against a real fake Firebase via draftbattle.js's test-injection hooks: one-click Will->All-Time climb (WINNING_SEED=12, still valid), per-row "Watch" replay, one Claim, progress-badge/cascade/claim-verification/daily-gate/throne-history-Inspect checks per prior batches. Bug-report throne-cascade scenario + same-mon cascade check. **Fixed a real intermittent flake:** the gauntlet-completion helper used a fixed 150ms wait after clicking "Challenge the Elite 4," but the gauntlet runs up to 5 sequential N=501 battle simulations, which can occasionally take longer under system load -- now polls for the results table to actually appear instead. Note: seeds found for a specific scenario are for TODAY'S DATE (Will/Koga's NPCs are period-keyed) -- may need re-finding later; the search method is documented inline. |


**Dev-only standalone smoke script** (not part of run.mjs/test:smoke; exercises the full Draft Battle / daily UI with a fake OFFLINE Firebase, i.e. no other entrants — daily.smoke.mjs above covers the multi-entrant matchup scenario): smoke-daily.mjs — also verifies the Yesterday/Today results round-trip and the #1 share-link format. Note: earlier notes also referenced smoke.mjs/smoke2.mjs, but neither exists in the current repo — that reference was already stale; throne.smoke.mjs/daily.smoke.mjs now cover what those were described as testing.

## Rules, docs, config

| File | Status | Notes |
|------|--------|-------|
| database.rules.json | ✅ | See "Data pipeline" above. |
| README.md | ✅ | Rewritten: intro paragraph covers Cycling Road (incl. Team Mode + host-disconnect resilience + room sharing), the Elite-4 gauntlet, and real move mechanics; repository layout tree updated to every current file; testing section documents both npm test and npm run test:smoke accurately. |
| CHANGE_TRACKER_v3.md | ✅ | The authoritative decision history — start here in any new chat. |
| TESTING_CHECKLIST.md | ✅ | Hands-on QA checklist, rewritten for this round's features: Daily Draft matchups/inspect/Cal rename, multiplayer room sharing (online + both Cycling Road modes), the Victory Road tier-preview fix, plus everything from the prior round (VR tier rework, GTR fixes, host-disconnect resilience, leaderboard draft links, daily share format, Elite-4 gauntlet flow, move-accuracy fixes, Fairy-type removal). |
| NEW_CHAT_GUIDE.md | ✅ | How to hand off to a fresh chat without losing context. |

---

## Known, disclosed gaps (not oversights — intentionally scoped out and documented)
- Online's clue cards don't visually match hot-seat's card styling yet (behavior is identical).
- Return/Frustration use a fixed optimal-happiness power (102) rather than a real friendship-based formula — no friendship stat exists in this draft context.
- Jump Kick/High Jump Kick's crash damage (1/8 max HP) is an estimate — no more Gen-2-specific figure found with higher confidence.
- Hidden Power is a fixed Normal-type 60-BP move. Its real per-Pokémon variable type/power can't be derived (no DVs/IVs here). The ideal "Hidden Power (X)" random-per-draft type is a cross-cutting draft-layer + UI feature, deferred as its own task (out of the sim-only scope).
- Weather: Solar Beam's power reduction in rain/sandstorm is not modeled (only its instant-charge in sun is); there are no weather-extending held items, so all weather lasts a fixed 5 turns.
- Substitute: drain/recoil use the move's computed damage rather than the amount the sub actually absorbed (minor over-heal/recoil when overkilling a sub); Pain Split ignores the sub and averages real HP; a confusion self-hit strikes the mon, not its own sub.
- The move audit (all 244 Gen-2 moves) is complete: every draftable move now behaves per Gen 2 within the switchless-1v1 scope, save the approximations listed here. Moves that are meaningless in a 1v1 with no switching/items/PP (switch-forcing, entry hazards, item theft, PP manipulation, gender, party-wide, dynamic-moveset copies) are banned from the draft pool rather than modeled — a deliberate scope boundary, listed in draft.js's BANNED_DRAFT_MOVES.
- Silhouette sprite assets (docs/img/silhouettes/<num>.png) were still owed by the user as of the original Phase 5 notes — the app degrades gracefully without them, but if they were never supplied, Pokedex/Draft silhouettes are still blank.
- #18 (a reported multiplayer rematch bug) was investigated extensively across two rounds but never reproduced by either side, and has been dropped.
- The daily results table gained a 5th (actions) column this round — worth a visual/mobile-width check since it hasn't been seen on a real device yet.
